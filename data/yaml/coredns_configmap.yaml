apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns
  namespace: kube-system
data:
  Corefile: |
    .:53 {
        log
        errors
        debug
        health
        ready
        forward . 8.8.8.8 1.1.1.1
        cache 30
    }
